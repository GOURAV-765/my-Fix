{
  "name": "society-management-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w frontend",
    "render-build": "npm run render-build -w backend",
    "start": "npm run start -w backend"
  }
}
